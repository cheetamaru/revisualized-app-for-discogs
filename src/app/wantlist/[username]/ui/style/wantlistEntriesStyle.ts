const cardContainerStyle: React.CSSProperties = {
    width: '100%'
}

const tableContainerStyle: React.CSSProperties = {
    padding: '15px 10px'
}

const cardsEmptyStyle: React.CSSProperties = {
    width: '100%',
    marginTop: 64
}

export const wantlistEntriesStyle = {
    cardContainerStyle,
    tableContainerStyle,
    cardsEmptyStyle
}